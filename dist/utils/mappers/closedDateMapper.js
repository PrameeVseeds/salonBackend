export const formatClosedDate = (closedDate) => ({
    id: closedDate.id,
    closedDate: closedDate.closed_date,
    reason: closedDate.reason,
    createdAt: closedDate.created_at,
    updatedAt: closedDate.updated_at,
});
//# sourceMappingURL=closedDateMapper.js.map